$('#flash').show();
$('#flash').html("<p>delete</p>");
if (<%= @list.tasks.count %> == 0) {
  $('.pakupaku-tasks').html("<p>Want PakuPaku</p>");
} else {
  $('.pakupaku-tasks').html("<%= escape_javascript(render @first_task) %>");
}
