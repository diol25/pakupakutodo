$('#flash').show();
$('#flash').html("<p>Add</p>");
setTimeout(function(){
  $('#flash').fadeOut();
  $('#flash').empty();
},1000);

if (<%= @list.tasks.count%> == 1) {
  $('.todos').html("<%= escape_javascript(render @task) %>");
}
